/**
 * Firebase Cloud Functions v2 (2nd Gen)
 *
 * Este arquivo está vazio pois a API de nutrição (USDA + Open Food Facts)
 * não precisa de proxy - ambas suportam CORS nativamente.
 *
 * Mantenha este arquivo caso queira adicionar outras Cloud Functions no futuro.
 */

// Exemplo de Cloud Function para uso futuro:
// const { onCall, HttpsError } = require('firebase-functions/v2/https');
//
// exports.exampleFunction = onCall(
//   { region: 'us-central1', cors: true },
//   async (request) => {
//     return { message: 'Hello from Cloud Functions!' };
//   }
// );
